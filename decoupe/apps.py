from django.apps import AppConfig


class DecoupeConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "decoupe"
    verbose_name = "Chiffrage découpe (laser / jet d'eau)"
